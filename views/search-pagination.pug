.pagination
    each page, index in pageLinkArray
        span
            a(class='page index' href=`/books/search/?q=${search}&page=${index}`) #{index + 1}
        p.backButton
            a.button(href="/books/?page=0") Exist Search