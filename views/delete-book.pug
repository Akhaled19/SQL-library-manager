extends layout 

block content
    h2 Do you really want to delete #{book.title}?
    form(action=`/books/${book.id}/delete`, method="post")
        p 
            input(type="submit" value="Delete Book")
        p
            a.button(href="/books/page/1") NO! Cancel.